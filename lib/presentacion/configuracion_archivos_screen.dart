import 'package:flutter/material.dart';

class ConfiguracionArchivosScreen extends StatelessWidget {
  const ConfiguracionArchivosScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}